import * as React from "react";
import {Card, CardContent, CardHeader} from "@material-ui/core";

export const Home = () => (
  <Card>
    <CardHeader title="Home Page" />
    <CardContent>Select a tab on the left</CardContent>
  </Card>
);
